
//function produce(){
 //   return 1000
//}
function getRandomInt() {
    return Math.floor(Math.random() * (10 - 0 + 1));
    }

function transform (p) {
   let res = Math.floor(Math.random() / (10 - 0 + 1));
 return res
}

function consume (p) {
    result.innerHTML = ("p=" (Math.random() / (10 - 0 + 1)));
}
